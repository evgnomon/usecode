# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

---
- name: "Check"
  tags:
    - check
  script: check
  args:
    chdir: "{{ workspace }}"

- name: "Build"
  tags:
   - build
  script: build
  args:
    chdir: "{{ workspace }}"
