# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

---
# tasks file for roles/ensure-podman

- name: Install podman
  ansible.builtin.apt:
    name: "{{ ensure_podman_packages }}"
    state: present
    update_cache: true
  when: ansible_facts['os_family'] == 'Debian'
