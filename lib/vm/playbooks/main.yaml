# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

---
- name: Build
  hosts: localhost
  gather_facts: false
  collections:
    - evgnomon.usecode
  roles:
    - z_build

- name: Release on tag
  hosts: localhost
  gather_facts: false
  collections:
    - evgnomon.usecode
  roles:
    - z_tag_release
