- name: Build
  hosts: localhost
  gather_facts: false
  collections:
    - evgnomon.usecode
  pre_tasks:
    - shell: |
        pip install uv
  roles:
    - role: z_secrets
    - role: z_defaults
    - role: z_build
