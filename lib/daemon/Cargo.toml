# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

[package]
name = "uc-daemon"
version = "0.1.0"
edition = "2021"
description = "WireGuard mesh + DNAT port forwarding manager"
license = "HGL"
publish = false

[[bin]]
name = "uc-daemon"
path = "src/main.rs"

[dependencies]
base64 = "0.23"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
tempfile = "3"
toml = "1"
x25519-dalek = { version = "3", features = ["static_secrets"] }

[profile.release]
strip = true
lto = true
codegen-units = 1
