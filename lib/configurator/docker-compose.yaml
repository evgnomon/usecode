# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

version: '3'
services:
  blueprint:
    build: .
    volumes:
      - cache:/home/nouser/.cache/blueprint

volumes:
  cache:
