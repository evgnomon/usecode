---
galaxy_info:
  author: evgnomon
  description: Link wslview (WSL open-in-Windows-explorer helper) into ~/.local/bin
  company:
  license: MIT
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all

dependencies:
  - role: local_bin
