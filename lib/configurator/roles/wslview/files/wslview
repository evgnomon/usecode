#!/bin/bash
# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

set -euo pipefail

case "${1:-}" in
  http://*|https://*) explorer.exe "$1" ;;
  *) explorer.exe "$(wslpath -w "$1")" ;;
esac
