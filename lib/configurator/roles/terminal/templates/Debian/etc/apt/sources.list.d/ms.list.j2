{% set arch = 'amd64' if ansible_facts['architecture'] == 'x86_64' else ansible_facts['architecture'] %}
deb [arch={{ arch }} signed-by=/etc/apt/keyrings/microsoft-prod.gpg] https://packages.microsoft.com/debian/{{ ansible_facts['distribution_major_version'] }}/prod {{ ansible_facts['distribution_release'] }} main
