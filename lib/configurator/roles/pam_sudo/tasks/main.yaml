# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

---
- name: Install sudo PAM configuration
  become: true
  ansible.builtin.template:
    src: "{{ ansible_facts['distribution'] }}/sudo.j2"
    dest: /etc/pam.d/sudo
    mode: '0644'
  tags:
    - pam_sudo
