#!/bin/sh
# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

repo_path=$(pwd)
org=$(basename $(dirname $repo_path))
repo=$(basename $repo_path)
formatted="${org}_${repo}"
echo $formatted
