#!/bin/sh

sudo -v
cd "$HOME/src/github.com/$USER/usecode/lib/configurator"
  ansible-playbook -K -i inventory.py  main.yaml -e "ansible_python_interpreter=$(which python3)" "$@"
cd - > /dev/null
sudo -K
