# License-Identifier: HGL
# Copyright (C) The Usecode Authors (see AUTHORS)

all:
  children:
    main:
      hosts:
        main:
    protected-servers: # Protected servers are only accessible from bastain host. we use Shadow as the bastain host.
      hosts:
        main:
    gpus:
      hosts:
        h100:
